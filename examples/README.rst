Examples
========

These examples demonstrate common ``braindec`` workflows.

The gallery is built without executing examples on Read the Docs because the
full NiCLIP demo downloads model assets and can require GPU resources. Build
locally with ``BRAINDEC_BUILD_GALLERY=1`` to execute examples and capture
outputs.
